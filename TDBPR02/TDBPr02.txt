Simulate recovery using undo, redo and undo-redo logging.


    -- Create the main table to store data
    -- CREATE TABLE MOCK_DATA (
    --     employeeID INT PRIMARY KEY,
    --     first_name VARCHAR(50),
    --     last_name VARCHAR(50),
    --     email VARCHAR(50),
    --     gender VARCHAR(50),
    --     JobTitle VARCHAR(50)
    -- );

    -- -- Create a logging table for Undo and Redo operations
    -- CREATE TABLE MOCK_LOG_TABLE (
    --     log_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    --     transaction_id NUMBER,
    --     operation_type VARCHAR2(10),
    --     record_id NUMBER,
    --     old_value VARCHAR2(50),
    --     new_value VARCHAR2(50)
    -- );

    -- -- Procedure to log Undo and Redo operations
    -- CREATE OR REPLACE PROCEDURE log_transaction(
    --     transaction_id NUMBER,
    --     operation_type VARCHAR2,
    --     record_id NUMBER,
    --     old_value VARCHAR2,
    --     new_value VARCHAR2
    -- ) AS
    -- BEGIN
    --     INSERT INTO MOCK_LOG_TABLE (transaction_id, operation_type, record_id, old_value, new_value)
    --     VALUES (transaction_id, operation_type, record_id, old_value, new_value);
    --     COMMIT;
    -- END;
    -- /


    at line 62 
    
-- Insert initial data into the MOCK_DATA table
-- INSERT INTO MOCK_DATA (employeeID, first_name, last_name, email, gender, JobTitle)
-- VALUES (1, 'Alice', 'Smith', 'alice@example.com', 'Female', 'Engineer');

-- INSERT INTO MOCK_DATA (employeeID, first_name, last_name, email, gender, JobTitle)
-- VALUES (2, 'Bob', 'Johnson', 'bob@example.com', 'Male', 'Manager');